#lang racket
(define (sum-digits-rec n)
  (define(helper leftover)
    (if (zero? leftover)
        0
        (+ (remainder leftover 10) (helper (quotient leftover 10)))
     )
  )
  (helper n)
)
(= (sum-digits-rec 123) 6)
(= (sum-digits-rec 12345) 15)





